#include <iostream>

using namespace std;

int main()
{
    int d, m;
    cin>>d>>m;
    switch (m) {
        case 1:
            if (d<=19) cout<<"Козерог"; else
            if (d>19) cout<<"Водолей";
        break;
        case 2:
            if (d<=18) cout<<"Водолей"; else
            if (d>18) cout<<"Рыбы";
        break;
        case 3:
            if (d<=20) cout<<"Рыбы"; else
            if (d>20) cout<<"Овен";
        break;
        case 4:
            if (d<=19) cout<<"Овен"; else
            if (d>20) cout<<"Телец";
        break;
        case 5:
            if (d<=20) cout<<"Телец"; else
            if (d>20) cout<<"Близнецы";
        break;
        case 6:
            if (d<=21) cout<<"Близнецы"; else
            if (d>21) cout<<"Рак";
        break;
        case 7:
            if (d<=22) cout<<"Рак"; else
            if (d>22) cout<<"Лев";
        break;
        case 8:
            if (d<=22) cout<<"Лев"; else
            if (d>22) cout<<"Дева";
        break;
        case 9:
            if (d<=22) cout<<"Дева"; else
            if (d>22) cout<<"Весы";
        break;
        case 10:
            if (d<=22) cout<<"Весы"; else
            if (d>22) cout<<"Скорпион";
        break;
        case 11:
            if (d<=22) cout<<"Скорпион"; else
            if (d>22) cout<<"Стрелец";
        break;
        case 12:
            if (d<=21) cout<<"Стрелец"; else
            if (d>21) cout<<"Козерог";
        break;
    }
    return 0;
}
